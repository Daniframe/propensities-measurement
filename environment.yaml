name: default-env

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.12
  - numpy
  - pandas
  - scipy
  - matplotlib
  - seaborn
  - pip
  - notebook
  - pip:
      - python-dotenv
      - pyyaml
      - tqdm
